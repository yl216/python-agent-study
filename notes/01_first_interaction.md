# 第 1 次学习：最小交互程序

今天的目标是写出一个 10 行以内的 Python 交互程序。

## 核心概念

- `input()`：等待用户输入，并把输入内容保存成字符串。
- `print()`：把内容显示在终端里。
- 变量：给一段数据起名字，后面可以继续使用。
- f-string：把变量放进字符串里，例如 `f"你好，{name}"`。

## 当前代码流程

1. 询问用户名字。
2. 询问用户今天想学什么。
3. 输出学习老师式的回应。

